/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { InstallationService } from "../installation.service";
import { InstallationCreateInput } from "./InstallationCreateInput";
import { InstallationWhereInput } from "./InstallationWhereInput";
import { InstallationWhereUniqueInput } from "./InstallationWhereUniqueInput";
import { InstallationFindManyArgs } from "./InstallationFindManyArgs";
import { InstallationUpdateInput } from "./InstallationUpdateInput";
import { Installation } from "./Installation";
import { EmployeeFindManyArgs } from "../../employee/base/EmployeeFindManyArgs";
import { Employee } from "../../employee/base/Employee";
import { EmployeeWhereUniqueInput } from "../../employee/base/EmployeeWhereUniqueInput";
@swagger.ApiBearerAuth()
export class InstallationControllerBase {
  constructor(
    protected readonly service: InstallationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: Installation })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: InstallationCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Installation> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "Installation",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"Installation"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        companyId: {
          connect: data.companyId,
        },

        equipmentId: data.equipmentId
          ? {
              connect: data.equipmentId,
            }
          : undefined,
      },
      select: {
        commissionDate: true,

        companyId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        equipmentId: {
          select: {
            id: true,
          },
        },

        id: true,
        installationStartDate: true,
        nameOfCompanyEqInstalled: true,
        nameOfTechnicalEngineerInstalled: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [Installation] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(InstallationFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Installation[]> {
    const args = plainToClass(InstallationFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Installation",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        commissionDate: true,

        companyId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        equipmentId: {
          select: {
            id: true,
          },
        },

        id: true,
        installationStartDate: true,
        nameOfCompanyEqInstalled: true,
        nameOfTechnicalEngineerInstalled: true,
        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: Installation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: InstallationWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Installation | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "Installation",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        commissionDate: true,

        companyId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        equipmentId: {
          select: {
            id: true,
          },
        },

        id: true,
        installationStartDate: true,
        nameOfCompanyEqInstalled: true,
        nameOfTechnicalEngineerInstalled: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: Installation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: InstallationWhereUniqueInput,
    @common.Body()
    data: InstallationUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Installation | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Installation",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"Installation"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          companyId: {
            connect: data.companyId,
          },

          equipmentId: data.equipmentId
            ? {
                connect: data.equipmentId,
              }
            : undefined,
        },
        select: {
          commissionDate: true,

          companyId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          equipmentId: {
            select: {
              id: true,
            },
          },

          id: true,
          installationStartDate: true,
          nameOfCompanyEqInstalled: true,
          nameOfTechnicalEngineerInstalled: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: Installation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: InstallationWhereUniqueInput
  ): Promise<Installation | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          commissionDate: true,

          companyId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          equipmentId: {
            select: {
              id: true,
            },
          },

          id: true,
          installationStartDate: true,
          nameOfCompanyEqInstalled: true,
          nameOfTechnicalEngineerInstalled: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id/employeeId")
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "read",
    possession: "any",
  })
  @ApiNestedQuery(EmployeeFindManyArgs)
  async findManyEmployeeId(
    @common.Req() request: Request,
    @common.Param() params: InstallationWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Employee[]> {
    const query = plainToClass(EmployeeFindManyArgs, request.query);
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Employee",
    });
    const results = await this.service.findEmployeeId(params.id, {
      ...query,
      select: {
        companyId: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        employeeAge: true,
        employeeDepartment: true,
        employeeEmail: true,
        employeeName: true,
        employeePhone: true,
        employeePosition: true,
        id: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post("/:id/employeeId")
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "update",
    possession: "any",
  })
  async createEmployeeId(
    @common.Param() params: InstallationWhereUniqueInput,
    @common.Body() body: InstallationWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      employeeId: {
        connect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Installation",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"Installation"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id/employeeId")
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "update",
    possession: "any",
  })
  async updateEmployeeId(
    @common.Param() params: InstallationWhereUniqueInput,
    @common.Body() body: EmployeeWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      employeeId: {
        set: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Installation",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"Installation"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id/employeeId")
  @nestAccessControl.UseRoles({
    resource: "Installation",
    action: "update",
    possession: "any",
  })
  async deleteEmployeeId(
    @common.Param() params: InstallationWhereUniqueInput,
    @common.Body() body: InstallationWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      employeeId: {
        disconnect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Installation",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"Installation"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
