/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { SparePartService } from "../sparePart.service";
import { SparePartCreateInput } from "./SparePartCreateInput";
import { SparePartWhereInput } from "./SparePartWhereInput";
import { SparePartWhereUniqueInput } from "./SparePartWhereUniqueInput";
import { SparePartFindManyArgs } from "./SparePartFindManyArgs";
import { SparePartUpdateInput } from "./SparePartUpdateInput";
import { SparePart } from "./SparePart";
@swagger.ApiBearerAuth()
export class SparePartControllerBase {
  constructor(
    protected readonly service: SparePartService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "SparePart",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: SparePart })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: SparePartCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SparePart> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "SparePart",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"SparePart"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        companyId: data.companyId
          ? {
              connect: data.companyId,
            }
          : undefined,

        equipmentId: data.equipmentId
          ? {
              connect: data.equipmentId,
            }
          : undefined,
      },
      select: {
        companyId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        equipmentId: {
          select: {
            id: true,
          },
        },

        estimatedExpireDate: true,
        hasSerialNumber: true,
        id: true,
        nameOfSparePart: true,
        partNumber: true,
        serialNumber: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "SparePart",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [SparePart] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(SparePartFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SparePart[]> {
    const args = plainToClass(SparePartFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "SparePart",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        companyId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        equipmentId: {
          select: {
            id: true,
          },
        },

        estimatedExpireDate: true,
        hasSerialNumber: true,
        id: true,
        nameOfSparePart: true,
        partNumber: true,
        serialNumber: true,
        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "SparePart",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: SparePart })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: SparePartWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SparePart | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "SparePart",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        companyId: {
          select: {
            id: true,
          },
        },

        createdAt: true,

        equipmentId: {
          select: {
            id: true,
          },
        },

        estimatedExpireDate: true,
        hasSerialNumber: true,
        id: true,
        nameOfSparePart: true,
        partNumber: true,
        serialNumber: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "SparePart",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: SparePart })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: SparePartWhereUniqueInput,
    @common.Body()
    data: SparePartUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<SparePart | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "SparePart",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"SparePart"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          companyId: data.companyId
            ? {
                connect: data.companyId,
              }
            : undefined,

          equipmentId: data.equipmentId
            ? {
                connect: data.equipmentId,
              }
            : undefined,
        },
        select: {
          companyId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          equipmentId: {
            select: {
              id: true,
            },
          },

          estimatedExpireDate: true,
          hasSerialNumber: true,
          id: true,
          nameOfSparePart: true,
          partNumber: true,
          serialNumber: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "SparePart",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: SparePart })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: SparePartWhereUniqueInput
  ): Promise<SparePart | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          companyId: {
            select: {
              id: true,
            },
          },

          createdAt: true,

          equipmentId: {
            select: {
              id: true,
            },
          },

          estimatedExpireDate: true,
          hasSerialNumber: true,
          id: true,
          nameOfSparePart: true,
          partNumber: true,
          serialNumber: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
